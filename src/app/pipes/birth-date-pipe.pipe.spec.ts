import { BirthDatePipePipe } from './birth-date-pipe.pipe';

describe('BirthDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new BirthDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
